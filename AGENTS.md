# B站弹幕看板项目规范

## 项目目标

- 构建一个在Apple Silicon Mac上运行的本地桌面程序。
- 程序匿名连接任意公开B站直播间，实时获取并存储直播事件。
- 主界面同时呈现实时弹幕与实时看板，关闭窗口后仍在菜单栏后台采集。
- 项目术语以`CONTEXT.md`为准，关键架构选择以`docs/adr/`为准。

## 抖音扩展

- 抖音能力复用现有Electron生命周期、会话、SQLite、看板和打包层，以独立平台适配器加入，不能覆盖或复制B站实现。
- 抖音匿名WSS在线PoC通过前只允许修改调研、规格、脱敏样本和独立协议边界，不能把未验证的签名或消息字段接入正式采集流程。
- 第一版抖音能力不读取、导入或要求用户提供账号Cookie，不绕过验证码、设备校验或平台访问控制。
- 平台匿名协议无法稳定提供的事件必须标记为不可用，不能按零值写入数据库或看板。

## 第一版范围

- 同一时间最多采集一个直播间。
- 支持房间号、直播链接和等待开播。
- 保存普通弹幕、礼物、醒目留言、直播状态、连接状态和热度。
- SQLite持久化保存历史会话，支持查看、搜索和整场删除。
- 自动重连并明确记录数据缺口。
- 生成Apple Silicon版本的`.app`和`.dmg`。

## 第一版不做

- B站官方开放平台、主播身份码和用户Cookie。
- 多直播间并发采集。
- Apple签名、公证、自动更新和Mac App Store。
- Excel导出、时间轴回放和多场对比。
- 用户进入、点赞、关注和排行榜事件。

## 工程约束

- 技术栈采用Electron、React、TypeScript和SQLite，详见`docs/adr/0001-electron-react-typescript-sqlite.md`。
- Electron主进程负责采集、存储和应用生命周期，渲染进程不能直接访问Node或数据库。
- B站返回数据必须在系统边界校验并转换为项目内部事件模型。
- 不能在源码、数据库、日志、文档或Git提交中保存Cookie、令牌、原始用户ID和完整原始消息。
- 用户去重使用本机随机盐生成的本地用户标识。
- 数据库写入不能阻塞WebSocket接收，实时界面不能无限保留DOM节点。
- 非平凡改动记录到`WORKLOG.md`。

## 验证要求

- 每项实现都要有与风险相称的自动测试。
- 采集协议必须包含可重复运行的脱敏数据包测试。
- 发布前完成12小时持续采集、每秒200条事件和单场100万条事件验证。
- 发布前验证窗口关闭后后台采集、异常中断恢复标记、本地数据持久化以及`.dmg`安装。

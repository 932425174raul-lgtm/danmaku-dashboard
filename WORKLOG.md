# 工作记录

## 2026-08-12 直播结束复盘

- 历史场次新增本地“直播复盘”，整场统计弹幕、活跃发言人数、人均发言和数据完整性。
- SQLite只读Worker新增结束场次聚合：12小时内5分钟一格，超长直播自动放大时间格并严格限制144格。
- 分时趋势同时展示弹幕量和去重活跃发言人数，弹幕用橙色、人数用蓝色、数据缺口用红色虚线，同时保留文字标签和图例。
- 新增完整文本精确匹配的重复弹幕TOP 5，附出现次数、已识别发言人数和首末时间。
- 调整建议只引用可追溯的峰值时段、重复弹幕和高峰集中度，不推断观众情绪、主播内容或因果关系。
- 界面明确说明活跃发言人数不是在线观众人数，数据缺口时提示趋势可能被低估。
- 使用合成数据完成1440px和430px实跑验收，复盘页无横向溢出，窄屏按概览、趋势、反馈和建议单列排列。

## 2026-08-12 Windows x64便携版

- 新增Windows 10与Windows 11 x64运行边界，macOS与Windows共用采集、SQLite Worker、IPC和React界面。
- B站与抖音桌面请求标识改为根据实际运行系统生成，Windows不再使用macOS标识。
- Windows的Chromium会话数据放在`userData/Chromium`，持久数据继续使用Electron `userData`，密钥密文由DPAPI保护。
- 新增ICO应用图标、Windows系统托盘图标、x64 PE打包、便携ZIP与产物检查。
- 发现macOS系统`zip`会破坏中文文件名，改用显式写入UTF-8中央目录标志的项目内Windows ZIP maker。
- GitHub Actions新增`windows-latest`构建，依次执行快速验证、Windows打包、运行时自检、两万条smoke基准和ZIP上传。

## 2026-08-12 棱镜编辑风界面重设计

- 将原有黑绿网格、荧光描边和英文科技标签替换为哑光近黑、暖灰白文字与低对比石墨层。
- 新增原创SVG棱镜组件，统一用于品牌、实时空状态与历史空状态；连续光谱只用于棱镜、导航选中线和趋势基线，不复刻具体专辑封面。
- Dock图标同步改为斜向入射的原创棱镜构图，移除旧版荧光绿对话气泡，保持应用内外视觉一致。
- GitHub仓库首页同步棱镜编辑风，新增自包含SVG封面和合成数据界面截图，并补全功能、平台差异、安装、数据位置、隐私、架构、开发、验证和已知边界。
- 统计卡片改为共享分隔线矩阵，弹幕用户名回归中性色，时间与数值保留等宽字体，正文和控件使用中文系统无衬线字体。
- 用合成空状态与高密度数据检查1440px、700px和历史页，装饰图形未压住文字，页面无横向溢出。

## 2026-08-12 完成版发布收口

- B站匿名协议在线验证通过，适配直播网页当前`spm_prefix=444.7`。
- 生产写入与历史读取全部移至writer/reader worker，写入采用20,000条/5秒上限及500条或100毫秒批次。
- 补齐数据缺口持久化、实际提交事件投影、存储错误闭环、FTS检索和每批5,000行删除。
- 实时看板加入30分钟趋势、高频词、活跃用户和缺口提示；实时IPC限制为200条和256KiB。
- 重新生成并验证Apple Silicon应用与DMG；包内2万条基准达到每秒18,409条。
- 历史弹幕改为键集分页，3个字符以上的查询走FTS5三元组索引，短查询保留只读worker中的参数化子串匹配。
- 实时活跃人数改用数据库精确投影；抖音复用受保护的本机HMAC键并保存16字节本地用户标识，不再按进程随机重置。
- 30分钟趋势补齐无弹幕的零值时间桶，并把重连缺口覆盖到实际持续区间；事件按接收时间落入趋势桶。
- 构建链新增本地Electron压缩包准备步骤，避免Forge重复访问网络；标准`package:mac`和`make:mac`均已复现通过。
- 最终`.app`的100万条writer工作进程黑盒基准提交完整，57.0秒完成，约17,540条/秒；DMG架构、ad-hoc签名、Fuse、asar、运行时和挂载验收通过。
- B站生产帧处理改用有上限的异步zlib/Brotli解压，并继续通过串行Promise链保序，避免大压缩帧阻塞Electron主事件循环。

## 2026-08-12

- 提高实时看板的色彩辨识度：六项指标按折射顺序使用红、橙、黄、绿、蓝、紫的数值与弱底色，30分钟趋势按时间从暖色过渡到冷色，高频词与活跃用户分别使用暖色和冷色；文字标签、位置和缺口虚线继续提供非颜色提示。
- 增加Windows 10/11 x64便携ZIP构建、Windows图标、平台匹配User-Agent、平台数据目录与真实Windows Runner产物验收；CI同时验证SQLite双Worker、正式EXE运行时探针和每秒200条smoke门槛。
- 修正Windows下`@electron/asar`使用反斜杠返回归档条目导致的校验器误报，归档路径统一规范化后再执行内容允许清单与禁止清单检查。
- 完成面向交付版的主界面：默认B站并可切换抖音，实时页同时提供有限500条的弹幕流、自动跟随控制、六项核心看板指标与匿名能力说明，窄窗口改用弹幕和看板页签。
- 接通本地历史场次、最近100条弹幕、场内搜索和整场二次确认删除；补齐读取、搜索、删除、空数据和协议能力不可用状态，620像素宽窗口采用列表到详情的单页推进。
- 新增平台切换、未知指标、历史搜索删除和新消息跟随组件测试；当前全部17个单元测试文件36项测试、TypeScript类型检查与ESLint均通过。

## 2026-08-01

- 完成抖音实时看板MVP：独立平台采集器、公开房间输入校验、临时匿名Electron会话、WSS外层与普通弹幕解码、本地HMAC用户去重、最近500条实时投影和窄IPC接口。
- 主界面改为抖音匿名实时信号看板，支持宽窗口左弹幕右指标和窄窗口页签；礼物、醒目留言、人数、热度与历史明确标为不可用。
- 端到端在公开活跃直播间验证建立连接、真实新弹幕入板、隐藏窗口后后台继续采集、700像素窄布局无横向溢出和正常停止。脚本不输出或落盘房间、用户、正文和原始帧。
- 修复采集异常后隐藏窗口未释放、WSS断开后状态仍显示正在采集、会话停止后时长仍继续增长以及首屏被空列表自动滚动遮住的问题，增加回归测试。
- 完成抖音匿名网页直播在线PoC。全新临时浏览器上下文在30秒内接收69个WSS二进制帧并全部解码，识别66个ACK和3个应用层心跳出站帧，连接未关闭。
- 匿名实测收到评论、点赞、进场、关注和房间统计消息方法；礼物、下播、断线游标与补发仍无证据，不接入正式采集流程。
- 建立独立`douyin-web-v1`外层解码边界和脱敏探针摘要，发现当前gzip声明位于重复头字段，且不使用的64位ID不能转为JavaScript安全整数。
- 增加完全人工构造的PushFrame、gzip Response、ACK标志、大`uint64`、出站ACK与心跳分类和摘要白名单测试；线上房间、用户、正文、Cookie、签名和原始帧均未写入仓库。

## 2026-07-29

- 确认第一版产品边界、采集生命周期、事件范围、实时看板指标、历史数据范围、异常恢复和性能目标。
- 确认采用Electron、React、TypeScript和SQLite，第一版只构建Apple Silicon本机发行版。
- 建立项目词汇表、首个架构决策、项目规范和Wayfinder开发地图。
- 完成B站网页直播弹幕协议验证。匿名WBI签名、节点发现、WebSocket鉴权、心跳、Brotli解压和多包解析均已实测。
- 建立`bilibili-web-v1`协议契约、失败边界、节点轮换与令牌刷新规则，以及不含线上原始消息和临时令牌的固定样本测试清单。
- 完成采集会话状态机，确定首次鉴权成功后创建历史、等待开播、后台采集、连续数据缺口、风控恢复、有序结束和异常中断恢复规则。
- 补充连接恢复与启动失败术语，并建立状态机验收场景。
- 完成直播事件模型与SQLite结构，确定规范化事实表、本地HMAC标识、金额单位、可重建投影、FTS5搜索、批量事务、迁移和可恢复整场删除。
- 使用本机SQLite3.51内存建库验证全部DDL和FTS5三元组分词器可以创建。
- 完成3种实时监控界面原型，确定宽窗口左侧弹幕与右侧看板、窄窗口内容页签、历史详情、数据缺口和菜单栏交互。
- 使用本机Chrome在1440px和390px宽度验证3种方案与5种状态，修复窄窗口状态操作挤压、触控尺寸和历史列表详情堆叠问题。
- 界面文字按`ui-copy-check`检查，匿名风控改为用户可理解的匿名访问受限，并补齐停止与删除确认中的目标和后果。
- 完成Electron进程与IPC边界，确定主进程拥有采集和生命周期，SQLite由唯一写worker与只读worker隔离执行，renderer只能访问沙箱化窄接口。
- 定义版本化IPC通道、运行时校验、实时推送节流、首次快照竞态处理、两阶段删除、窗口隐藏继续采集和10秒安全退出流程。
- 完成高吞吐与实时聚合原型，使用脱敏合成事件验证单写SQLite worker、500条或100毫秒批次、固定容量投影和节流renderer载荷。
- 100万条最终验证约26,372条/秒，writer提交P99为55.91毫秒；120秒定速200条/秒精确提交24,000条，队列峰值24，主线程事件循环P99为15.57毫秒。
- 原型发现并修正FTS直接关联造成的2秒级查询，改为物化匹配ID后热查询P95为9.07毫秒；1至2字符使用参数化子串回退。
- 高频词改为128项Space-Saving候选并保存误差上界，12小时1,382,400条生产发布门槛和常数内存采样方式已确定。
- 完成Apple Silicon打包与本地数据规格，固定Electron 43.2.0、Electron Forge 7.11.2、`darwin-arm64`、ad-hoc签名和macOS 13最低版本。
- 明确Application Support、Caches和Logs目录，使用`safeStorage`保护本地HMAC键，以SQLite活动会话作为唯一异常恢复事实，并停用第一版`crashReporter`。
- 确定首次打开、Gatekeeper人工放行、10秒有序退出、手工升级、默认保留数据的卸载和完整清理流程。
- 本机确认构建工具存在并核实npm锁定版本；完整烟测受Electron官方压缩包经本地代理下载过慢影响，已转为编码第一阶段的产物硬验收，不记为通过。
- 完成测试、诊断与脱敏日志规格，确定Vitest、Testing Library与Playwright的分层职责，以及固定fixture、故障注入、Electron集成、产物和人工验收边界。
- 为协议、状态机、SQLite、IPC、实时聚合、renderer、后台生命周期和macOS打包建立完整测试矩阵，统一100万条、每秒200条与12小时发布门槛。
- 日志采用固定事件联合类型和运行时schema白名单，5 MiB轮转、25 MiB总上限、7天保留，不记录房间、用户、正文、搜索、凭据、SQL、路径或原始异常。
- 确定匿名访问受限和数据缺口的脱敏事件链、用户主动诊断摘要，以及覆盖SQLite、IPC、日志、renderer、测试产物和正式asar的隐私canary门槛。
- 核实Playwright Electron仍为实验能力，Spectron已归档；第一版关闭Crashpad、Node诊断报告、heap snapshot、net log和线上trace，避免敏感内存、环境与网络内容落盘。
- 汇总全部专题结论，形成第一版实施规格，固定目录边界、依赖版本、两阶段数据库迁移、M0至M9里程碑、验收命令和发布证据。
- 统一正式产物的Electron Fuse，关闭Node调试参数，Playwright只驱动未打包测试入口；最终`.app`改用固定、无网络、临时数据库的黑盒验证参数。
- 校准性能命令边界：快速`smoke`使用项目锁定的Electron运行生产模块，100万条、每秒200条和12小时只接受当前提交的最终`.app`。
- 补齐菜单栏诊断摘要入口、最小窗口尺寸和正式产物隐私允许清单，消除测试、界面、IPC与打包规格间的不一致。

## 2026-07-31

- 启动抖音公开直播间扩展调研，匿名页面请求已取得公开房间的长`room_id`；官方玩法能力与网页私有协议边界已经区分，WSS在线收包尚未验证，暂不进入采集器编码。
- 开始M0脚手架实施，建立精确依赖、TypeScript、ESLint、Prettier和Vitest基础配置。
- 首次严格Fuse构建确认Electron 43已包含第9个`WasmTrapHandlers`，而`@electron-forge/plugin-fuses@7.11.2`的peer范围仍停留在只能识别8个Fuse的`@electron/fuses@^1.0.0`。移除该插件，改由Forge的`packageAfterCopy`钩子直接调用`@electron/fuses@2.1.3`，显式配置全部9个Fuse，没有关闭严格模式或使用强制安装参数。
- `jsdom@30.0.1`要求Node.js`24.15.0`及以上，与项目固定的`24.14.1`冲突；测试基线改为兼容的`jsdom@29.1.1`。
- Forge官方DMG maker间接依赖的`appdmg`原生模块不能在当前Node.js与macOS命令行工具组合下编译；改为项目内Forge maker调用系统`hdiutil`，继续生成ULFO格式和`/Applications`安装入口，同时移除旧原生依赖。
- macOS 26的`iconutil`无法把自身导出的系统iconset重新编码，应用图标改用无原生依赖的`png2icons@2.0.1`生成ICNS，SVG和1024像素源图保留在仓库中。
- 首次产物黑盒启动发现Electron 43 arm64发行包只有通用V8快照，启用`LoadBrowserProcessSpecificV8Snapshot`会在业务代码运行前致命退出，因此该Fuse显式设为`false`。同时在签名前删除Electron模板中项目未使用的媒体、蓝牙权限用途声明和任意ATS传输覆盖。
- 完成M0安全桌面外壳、沙箱窗口、窄preload接口、`app://`协议、启动参数校验、SQLite运行时探针、图标和最小状态台界面。正常启动不访问网络，也不创建业务数据库。
- 完成Apple Silicon`.app`和ULFO格式DMG，自动验收arm64、macOS 13、ad-hoc签名、全部9个Fuse、asar允许内容、DMG完整性，以及主进程和双Worker的SQLite、FTS5 trigram、WAL、备份与`safeStorage`。
- `npm ci`从锁文件复现安装通过；`npm audit --omit=dev`为0项漏洞。完整审计仍报告31项仅开发工具链漏洞，其中1项critical来自Forge间接使用的旧`tar`，当前Forge 7.11.2依赖树没有不破坏锁定工具链的上游修复，正式asar不包含这些开发依赖，后续升级Forge时重新审计。
- 最终重建时确认ad-hoc签名的代码身份变化会让`safeStorage.isEncryptionAvailable`与加解密再次等待macOS钥匙串授权，无界面黑盒命令无法稳定完成。M0自动探针改为验证正式运行时包含项目所需的异步`safeStorage`API，系统加密可用性、真实加解密和跨版本连续性继续作为发布人工门槛；此前同机首次加解密往返已验证成功。
- M0强制代码审查未发现P0。按审查结论将验证与性能模式的Electron目录迁入系统临时目录并在退出时清理，让writer连接保持开启时启动reader验证WAL并发读取，同时回读`foreign_keys=1`和`busy_timeout=5000`。
- 同轮加固拒绝全部renderer页面导航与下载，固定`app://renderer`空端口并移除生产CSP中的`unsafe-inline`；产物验收新增thin arm64、ad-hoc身份、ULFO格式、DMG只读挂载内容和`/Applications`符号链接验证，修正plist缺键检查的失败放行，并确保备份数据库异常时关闭连接。

## 2026-08-12

- 将生产SQLite写入扩展为独立writer worker命令循环，数据库路径只通过worker启动配置传入，消息边界拒绝SQL和任意路径。
- 实现20,000条与5秒双上限写入队列，按500条或100毫秒先到者分批，严格保证单事务在途并在关闭前刷新队列。
- writer返回实际插入的`committedEvents`，重复事件不再进入实时投影；补全数据缺口的幂等开启、关闭及会话终态结算。
- 历史列表、弹幕键集分页和搜索迁入只读reader worker，Electron主进程不再创建`LocalStore`或持有SQLite连接。
- 整场删除改为writer worker先设置`deleted_at_ms`立即从界面隐藏，再每批最多5,000行清理事实表与投影，批次间主动让出事件循环。
- 采集状态快照保留本场缺口数量、当前缺口起点和最近缺口原因；连续恢复只更新末次原因与重试次数，重新鉴权成功当即关闭缺口。
- B站业务事件与热度回调改为等待持久化确认，写入拒绝时关闭当前WebSocket并进入存储恢复；抖音协议解码与业务回调错误分开处理，错误后清理会话与采集器并允许重新开始。
- 异常中断恢复会以最后检查点关闭未完成数据缺口，标记为未恢复并结算缺口指标。
